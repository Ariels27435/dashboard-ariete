# Configuración del servidor
PORT=3001
NODE_ENV=production

# MongoDB Atlas - Reemplaza con tu URL de MongoDB Atlas
MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/ariete_db?retryWrites=true&w=majority

# JWT Secret - Genera uno seguro para producción (puedes usar: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=tu_jwt_secret_super_seguro_aqui

# CORS - URLs permitidas (separadas por comas)
# Para desarrollo local y producción
CORS_ORIGIN=http://localhost:5173,https://tu-dominio.vercel.app

# API Key para ESP32 - Usa una clave segura en producción
ESP32_API_KEY=ariete-esp32-2025

# Variables opcionales
JWT_EXPIRES_IN=7d

